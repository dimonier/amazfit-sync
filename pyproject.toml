[project]
name = "amazfit-sync"
version = "0.1.0"
description = "Sync Amazfit tracking data into JSON and Obsidian."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "requests",
]

[project.scripts]
amazfit-sync = "amazfit_sync.pipeline:main"
